import 'package:flutter/material.dart';

///
class ListStoriesPhoneView extends StatelessWidget {
  ///
  const ListStoriesPhoneView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('liststores'),
      ),
    );
  }
}
