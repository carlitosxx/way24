import 'package:flutter/material.dart';

/// Widget para espaciar en horizontal de tamaño mediano
class HorizontalSpacerMedium extends StatelessWidget {
  /// constructor
  const HorizontalSpacerMedium({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 16,
    );
  }
}
