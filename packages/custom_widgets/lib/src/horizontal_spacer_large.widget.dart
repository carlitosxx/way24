import 'package:flutter/material.dart';

/// Widget para espaciar en horizontal de tamaño large
class HorizontalSpacerLarge extends StatelessWidget {
  /// constructor
  const HorizontalSpacerLarge({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 24,
    );
  }
}
